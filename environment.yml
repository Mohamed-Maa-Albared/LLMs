channels:
  - conda-forge
dependencies:
  - pip
  - pip:
      - requests
      - flask
      - ollama
      - langchain
      - langchain-community
      - unstructured
      - chromadb
name: llm_env